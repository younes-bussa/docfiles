;;; my-misc.el -*- lexical-binding: t; -*-

(use-package which-key
  :init
  (which-key-mode 1)
  :config
  (setq which-key-idle-delay 0.5))

;; Add magit, doom-modeline, etc. here later

(provide 'my-misc)
